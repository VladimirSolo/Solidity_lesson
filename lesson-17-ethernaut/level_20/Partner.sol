// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract Partner {
    receive() external payable {
        // assert(false);
        while (true) {}

        // assembly {
        //     invalid()
        // }
    }
}
